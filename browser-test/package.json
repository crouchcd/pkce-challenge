{
  "name": "browser-test",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && esbuild index.js --bundle --outfile=index.out.js"
  },
  "dependencies": {
    "pkce-challenge": "file:.."
  }
}
